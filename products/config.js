window.MFB_PRODUCTS_CONFIG = Object.freeze({
  // Add the India Apps Script URL here once the catalogue API is deployed.
  API_URL: "",
  CART_STORAGE_KEY: "mfb_in_cart_v3",
  DEFAULT_MINIMUM_ORDER_KG: 0,
  INITIAL_RENDER_COUNT: 18,
  RENDER_BATCH_SIZE: 15,
  IMAGE_TIMEOUT_MS: 3500,
  API_TIMEOUT_MS: 12000
});
